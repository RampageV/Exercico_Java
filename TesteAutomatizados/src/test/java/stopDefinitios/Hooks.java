package stopDefinitios;

import io.cucumber.datatable.dependency.com.fasterxml.jackson.annotation.JacksonInject.Value;
import io.cucumber.java.After;
import io.cucumber.java.Before;
import static utils.Utils.*;

public class Hooks {
/*
	@Before() // "@Before(order = 1, value = "@Login")": Só vai exercutar o Before os que tem a tec login. A order informa qual vai ser o primeiro a ser exercutado.
	public void setUp() { // Antes de começa qualquer teste, precisa abrir o navergado e o sistema.

		acessarSistema();
	}

	@After
	public void tearDown() { // O After é a mesma coisa do before, porém, o after é depois que é exercutado e se tu usar o ordem nele, ele vai começa da ordem decrecente: 3,2,1
        driver.quit();
	}
*/
}
